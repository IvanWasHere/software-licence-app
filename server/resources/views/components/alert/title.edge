@let(titleFromSlot = await $slots.main())
@let(classes = ['toast-title'])

{{-- Slot markup is printed as-is; a `text` prop is escaped (see badge.edge). --}}
<h2 {{ $props.except(['text']).merge({ class: classes }).toAttrs() }}>@if(titleFromSlot.trim()){{{ titleFromSlot }}}@else{{ $props.get('text') ?? '' }}@end</h2>
